apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "datakit-operator.fullname" . }}-test-connection"
  labels:
    {{- include "datakit-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "datakit-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
