---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "datakit-operator.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "datakit-operator.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
